<UserControl x:Class="Melville.WpfControls.FileDownloadBars.FileDownloadBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Melville.WpfControls.FileDownloadBars"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="{Binding BarBrush}" BorderThickness="1">
        <local:HarlequinTextBlock Margin="1" Text="{Binding Message}" 
                                  BarBrush="{Binding BarBrush}" NonBarBrush="{Binding NonBarBrush}"
                                  Offset="{Binding Offset}"/>
    </Border>
</UserControl>
