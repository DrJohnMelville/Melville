<UserControl x:Class="Melville.Wpf.Samples.KeyCounters.KeyCounterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Melville.Wpf.Samples.KeyCounters"
             xmlns:eb="clr-namespace:Melville.MVVM.Wpf.EventBindings;assembly=Melville.MVVM.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             PreviewKeyDown="{eb:EventBinding KeyPress}">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:KeyCount}">
            <TextBlock><TextBlock Text="{Binding Name}"/> <TextBlock Text="{Binding Count}"/></TextBlock>
        </DataTemplate>
    </UserControl.Resources>
    <DockPanel>
        <Button Click="{eb:EventBinding Reset}">Clear</Button>
        <ListBox ItemsSource="{Binding Counts}"/>
    </DockPanel>
</UserControl>
